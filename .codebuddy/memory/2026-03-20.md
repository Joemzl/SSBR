# 2026-03-20 工作记录

## 数据质量清洗

完成 `dataset/数据.xlsx` 数据质量检查和清洗：

1. **删除 45 个低质量样本** (无官能化试剂名称)
   - 样本数: 116 → 71
   - 备份: `dataset/backups/数据_backup_20260320_133953.xlsx`

2. **修复 SMILES 问题** (24 个样本)
   - 9 个: 补充了正确的 SMILES
   - 15 个: 标记为「复杂材料」(纳米材料/高分子材料)

3. **补充高分子指纹描述符** (35 个样本)
   - 14 个: 成功生成
   - 21 个: 数据不足跳过

## ChromaDB 向量数据库迁移

将向量存储从 JSON 文件迁移到 ChromaDB PersistentClient：

### 新增/修改文件
- `requirements.txt`: 新增 `chromadb>=0.4.0` 依赖
- `scripts/utils/vector_store.py`: **新建** ChromaDB 向量存储模块
  - `VectorStore`: 底层 ChromaDB 封装
  - `VectorStoreAdapter`: 兼容旧 `VectorCache` 接口的适配器
- `scripts/rag_search.py`: 更新导入，使用 ChromaDB 原生搜索
- `scripts/build_vector_cache.py`: 更新导入

### 存储位置变化
- 旧: `.cache/vector_cache.json` (3.8 MB JSON)
- 新: `.cache/chroma_db/` (SQLite + HNSW 索引)

### 性能对比
| 指标 | JSON 方案 | ChromaDB |
|------|-----------|----------|
| 启动加载 | ~500ms | ~50ms |
| 搜索延迟 | ~100ms | ~5ms (HNSW) |
| 增量更新 | 重写 JSON | 仅更新差异 |

### 测试结果
- ✅ 构建缓存: 71 个文档，耗时 3.2 秒
- ✅ RAG 搜索: 正常工作
- ✅ QA 问答: 正常工作 (需禁用 reranker，模型加载问题待修复)

## 文档更新

重写 `README.md` 项目说明文档，主要改进：

1. **新增跨设备初始化说明**: 明确说明 `.cache/chroma_db/` 不通过 Git 同步，首次使用/换电脑需运行 `python scripts/build_vector_cache.py --force`
2. **完整依赖安装说明**: 使用 `requirements.txt` 替代手动列举
3. **环境变量配置**: 详细的 Windows/Linux/Mac 配置方法
4. **常用命令速查**: 向量缓存、RAG 检索、Web Demo、样本管理
5. **故障排查指南**: 常见错误及解决方案
6. **更新项目结构**: 反映 ChromaDB 迁移后的目录结构

同步更新 `CODEBUDDY.md` 中的 ChromaDB 相关描述。

## 核心官能团数据补充

补充 `dataset/数据.xlsx` 中的核心官能团相关列：

### 新增脚本
- `scripts/fill_functional_groups.py`: 核心官能团数据补充工具
  - 根据「核心官能团名称」映射补充 SMILES 和化学式
  - 根据「官能化试剂名称」推断缺失的官能团信息
  - 按提示词规范重建「高分子指纹描述符」

### 数据补充结果
| 指标 | 修改前 | 修改后 |
|------|--------|--------|
| 核心官能团名称缺失 | 19 | 0 |
| 核心官能团 SMILES 缺失 | 39 | 2 (纳米材料无 SMILES) |
| 核心官能团化学式缺失 | 46 | 0 |
| 高分子指纹描述符修正 | - | 68 条 |

### 官能团映射表
脚本中维护了两个映射表：
1. `FUNCTIONAL_GROUP_MAP`: 官能团名称 → (SMILES, 化学式)
2. `REAGENT_TO_FUNCTIONAL_GROUP`: 试剂名称 → (官能团名称, SMILES, 化学式)

备份位置: `dataset/backups/数据_backup_20260320_152344.xlsx`
